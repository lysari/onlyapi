# Service — ClusterIP for internal routing
apiVersion: v1
kind: Service
metadata:
  name: onlyapi-svc
  namespace: onlyapi
  labels:
    app.kubernetes.io/name: onlyapi
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: onlyapi
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
